// this file will contain a reference to the Figma file (and frames) that we sync with
// what you see below is just an example of what it could look like

// Notice: this simple configuration is under the assumption thet all the assets will be in the same page and we only want export certain frames (like it is at the moment)
// if later we will discover we need to be more specific (eg. file/page/frame) we will adopt a more structured/explicit configuration format

export const figmaFileURL = "https://www.figma.com/file/TLnoT5AYQfy3tZ0H68BgOr/Flight-Icons?node-id=350%3A2";
export const figmaFileFrames = [
    // base set
    "205-A1",
    // 3rd party services
    "388-A1513",
]
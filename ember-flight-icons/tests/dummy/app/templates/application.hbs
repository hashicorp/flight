{{page-title "Flight Icons: An Ember Addon"}}

<h1 id="title">Flight Icons: An Ember Addon</h1>

<section>
  <h2>Example:</h2>
<ul>
  <li>
    This should be a large "activity" icon: <FlightIcon @name="activity" />
  </li>
  <li>This should be a small "activity" icon: <FlightIcon @name="activity" @size="16" /></li>
  <li>This should be a small "zap" icon: <FlightIcon @name="zap" @size="16" @isInlineBlock={{false}} /></li>
  <li>This should be a red large "activity" icon: <FlightIcon @name="activity" @color="red" /></li>
</ul>
</section>

We are here:
{{#each @model as |meta|}}
  {{!-- {{log meta}} --}}
  {{!-- this logic but put in js or helper so can access here --}}
  {{!-- {{log meta.Name.replace(regSize, '')}} --}}
  {{!-- returns `volume-24` we want volume ... we want some regex that removes the "-##"" --}}
  {{!-- ^(.+?)24(.+?)$  --}}
  <FlightIcon @name={{meta.Name}} />
{{/each}}
